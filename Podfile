# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SimulateLocation' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SimulateLocation
  pod 'Masonry'
  pod 'extobjc'
  
  target 'SimulateLocationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SimulateLocationUITests' do
    # Pods for testing
  end

end
